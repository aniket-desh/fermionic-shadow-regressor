# Core
torch>=2.0
numpy>=1.24,<2.0
scipy>=1.10
h5py>=3.8
pyyaml>=6.0
tqdm>=4.65
matplotlib>=3.7

# Molecular Hamiltonians (data generation)
pennylane>=0.35
# Required for HF orbital-energy features (the orb recipe). Without pyscf,
# compute_hf_orbital_energies falls back to zeros and validate_orb_features()
# aborts training. Mandatory wherever datagen runs (cluster or RunPod).
pyscf>=2.3

# Tests
pytest>=7.0
